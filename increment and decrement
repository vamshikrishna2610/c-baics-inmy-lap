//increment and decrement values
// unary operations
#include<stdio.h>
  int main()
  {
      int a=12,b=13;
      float c=10.5,d=16.5;
      
      printf("++a=%d\n",++a);
      printf("--b=%d\n",--b);
      printf("++c=%f\n",++c);
      printf("--d=%f\n",--d);
      
      return 0;
  }
